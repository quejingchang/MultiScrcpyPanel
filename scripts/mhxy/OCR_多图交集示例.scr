# =====================================================================
# OCR 多图「位置交集」示例（模板，按需修改后使用）
# ---------------------------------------------------------------------
# 语法：
#   OCR <图A> <图B> [更多图...] [MAXERR 0.15] [TIMEOUT 0] [RETRY 1] [WAIT 300] [DX n] [DY n] [CENTER]
#
# 规则：
#   1) 每张图在屏幕里各自识别出一个命中框（包围盒）。
#   2) 取所有命中框的「位置交集」作为最终点击区域。
#   3) 区域内随机取一点点击（加 CENTER 则取交集中心）。
#   4) 任一图未命中 → 按 RETRY/TIMEOUT 重试；多图无交集 → 重试。
#
# 适用：两张图是「同一个可点击区域」的相邻两部分（例如一个按钮的
#       左半图标 + 右半文字），它们的包围盒相交，交集中随机点击更稳。
#       若两图相距很远（包围盒不相交），请用单图 OCR 分别点。
#
# 下面这行是示例（请把 图A/图B 换成你裁好的、会相交的图标）：
# OCR 图A.png 图B.png MAXERR 0.18 CENTER
# =====================================================================
