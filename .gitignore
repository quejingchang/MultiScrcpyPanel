# ---- .NET 构建产物 ----
bin/
obj/
*.user
*.suo
.vs/

# ---- 运行期产物 ----
logs/
config/settings.json

# ---- 运行必要资源（入库：scrcpy-server.jar / platform-tools adb 等，设备连接依赖）----

# ---- 外部下载依赖（由 tools/ 脚本下载或构建，不入库）----
native/
opencv/

# ---- 本机同步 / 临时产物 / 日志（不入库）----
.sync-conflict-backup/
.writetest
*.log

# ---- 本地游戏截图素材（不入库；注意用 /mhxy/ 限定根目录，避免误伤 scripts/mhxy 脚本）----
/mhxy/

